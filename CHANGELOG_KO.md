# 업데이트 이력

## 1.1.2 - 2026-09-25

- 1.0.7에서 도입된 Windows UI 글꼴 로딩의 시작 시 접근 위반을 수정했습니다. TTC 글꼴 모음을 통째로 읽고 Windows가 선택한 글꼴을 정확히 지정하여, 내부 테이블의 기준 위치가 어긋나지 않게 했습니다. Windows 기본 글꼴과 기존 한국어·중국어 표시 범위는 유지합니다.
- TTC 글꼴 선택, 잘못된 디렉터리 거부, 반복 로딩 후 글꼴 메모리·GDI 핸들 정리를 검사했습니다. 기존 코드의 접근 위반을 Microsoft JhengHei UI, MS UI Gothic, Yu Gothic UI에서 재현했고, 수정 경로와 전체 회귀 검사 31개가 통과했습니다. 제보자의 게임·크래시 로그를 통한 동일 원인 확인은 필요합니다.

## 1.1.1 - 2026-09-19

- 실행 중인 MCM 속성·전역 변수·배열을 변경하기 전에 소유 모드를 확인합니다. SkyUI의 페이지와 키맵 유형이 확인된 경우 유효한 첫 옵션 ID 0도 처리합니다.
- INI/TOML 편집 시 섹션과 설정명으로 대상을 다시 찾습니다. 줄이 이동해도 선택한 설정만 변경하고, 기존 값 확인과 같은 섹션의 중복 대상 거부를 유지합니다.
- 보조키 플래그와 복합키 필드를 JSON 객체·INI/TOML 섹션·YAML 부모별로 구분합니다. 별도 필드에 저장된 보조키를 변경 미리보기에 유지하며, 저장할 수 없는 보조키 변경은 쓰기 전에 거부합니다.
- 기존 256KiB 미리 읽기 범위 뒤에 있던 키와 새로 추가된 설정 파일도 감지합니다. 세이브 로드 후 캐시를 활용해 한 번 갱신하고 실행 중인 키 값을 다시 수집하며, 취소 중인 이전 스캔이 끝난 뒤 진행합니다.
- 충돌 상대 목록의 제곱 비례 중복 저장을 공유 그룹과 필요 시 조회 방식으로 바꿨습니다. 모든 충돌 항목과 상대 목록을 유지하고 화면 갱신 시 분석 결과를 재사용합니다.
- UTF-16 설정의 키는 계속 표시하되 지원하지 않는 파일 편집은 읽기 전용으로 정확하게 표시합니다.
- JSON 서식 변경 후에도 백업 대상을 식별하고, 키 재지정 후에도 사용자 지정 기능명을 유지합니다. 1.1.0 이름 설정을 이전하며 1.0.9/1.1.0 변경 이력을 보존합니다.
- UHM 자체 INI의 UTF-8 BOM을 처리하고, 화면 배율·불투명도에 NaN·무한대 값이 들어가지 않도록 수정했습니다.
- 외부 API에서 UI 처리 전에 열기·닫기가 연속 호출되면 마지막 요청을 따릅니다. 닫기 직후 다시 열기가 무시되던 문제를 수정했습니다.
- 한글 MSVC 메시지로 Ninja의 헤더 의존성 추적이 깨져 이전·새 자료 구조가 섞이던 빌드 문제를 수정했습니다. 해당 경로의 회귀·메모리 할당 검사를 추가하고 기존 팝업·입력 해제·윈도우 글꼴 검사를 유지했습니다.

## 1.1.0 - 2026-09-19

- 세이브 파일 삭제를 게임 로딩으로 잘못 처리해 다음 로드까지 스캔·단축키 변경이 막히던 문제를 수정했습니다.
- JSON/JSONC 설정을 전체 객체·배열 경로로 구분합니다. 같은 이름과 키를 가진 기능을 각각 유지하고, 서식이 바뀌어도 선택한 설정만 편집·백업 복원합니다. 주석이나 다른 문자열을 잘못 수정하지 않도록 보완했습니다.
- MCM 비동기 처리 완료와 실제 값 적용을 확인한 뒤 성공 이력을 기록합니다. 기존 속성 직접 변경·파일 저장 경로는 유지하며 갱신 필요·처리 대기·미확인 상태를 구분해 안내합니다. 완료된 MCM 변경이 실패하면 연결 문서를 복구하고, 아직 실행 중인 요청이 시간 초과되면 뒤늦은 적용과 충돌할 수 있는 복구 대신 미확인으로 알립니다.
- MCM 대기 상태를 완료·시간 초과·세이브 전환 때 정리합니다. 콜백은 작업이나 Papyrus 객체를 붙잡지 않으며, 늦은 결과가 다른 편집창을 닫지 않습니다. 변경마다 대기 스레드를 만들던 방식을 기존 처리 루프 사용으로 바꿨습니다.
- 윈도우 글꼴 아틀라스에 누락된 중국어 UI 글자 3개를 추가했습니다. 기존 글꼴 선택과 작은 문자 범위는 유지합니다.
- 1.0.9의 단축키 변경 이력은 보존하고 이전 스캔 캐시를 갱신합니다. 정확한 설정 편집·백업 호환·MCM 완료 및 참조 해제·전체 한중 UI 문자 검사를 추가하고, 릴리즈 빌드에서도 ESC 검사가 실행되도록 보완했습니다.

## 1.0.9 - 2026-09-19

- 1.0.8에서 UHM을 처음 열 때 발생하는 CTD를 수정했습니다. 팝업 목록이 비어 있을 때 존재하지 않는 첫 항목을 읽지 않고 안전하게 처리합니다.
- 1.0.8의 마우스 휠 및 메뉴 종료 수정과, 다시 열 때 이전 입력·팝업을 정리하는 동작을 유지했습니다.
- 실제 ImGui 라이브러리로 첫 열기, 빈 목록 정리, 모달·중첩·아직 그리지 않은 팝업 및 100회 재열기를 검증하는 회귀 테스트를 추가했습니다. 기존 접근 위반을 로컬에서 재현했으며, SE/AE 클린 빌드와 자동 테스트 26개를 통과했습니다. 실제 게임 확인은 별도로 필요합니다.

## 1.0.8 - 2026-09-18

- UHM을 닫아도 내부 열림 상태가 남아 휠 입력을 계속 소비하고, 세이브 재로드 전까지 캐릭터 카메라 줌을 막을 수 있는 경로를 수정했습니다. 별도의 렌더링 열림 표시를 제거하고, 닫는 중인 메뉴의 추가 렌더링·커서 표시 예약을 차단했습니다.
- 일반 닫기·강제 닫기·메뉴 해제 시 팝업·입력 캡처·대기 중인 마우스 입력·커서 소유 상태를 정리합니다. 오래된 표시·커서 예약 작업을 취소하며 다른 메뉴가 사용하는 커서는 유지합니다.
- 닫기 버튼·ESC·열기 단축키·외부 닫기 API가 같은 정리 경로를 사용합니다. 다시 열 때 이전 ImGui 입력과 팝업도 초기화합니다.
- 지연 종료, 강제 종료, 중복 정리, 오래된 예약 작업 및 100회 개폐에 대한 회귀 테스트를 추가했습니다. SE/AE 클린 빌드와 자동 테스트 25개를 통과했으며, 제보된 카메라 줌 증상의 실제 게임 확인은 필요합니다.
- 지원 범위는 기존 SE 1.5.97 및 명시된 AE 1.6.x 버전과 같습니다. Skyrim 1.7.x와 VR은 지원하지 않습니다.

## 1.0.7 - 2026-09-18

- 입력 캡처가 한 프레임의 전체 이벤트를 처리하도록 수정했습니다. 좌우 보조키 및 키보드 보조키+마우스 입력을 보존하고, UHM 열기 키가 비활성화됐거나 렌더러가 준비되지 않으면 다른 모드 입력을 막지 않습니다. 팝업 클릭·휠 입력도 전달합니다.
- 키 우클릭 메뉴에서 모든 기능을 스크롤하며 선택할 수 있습니다. 모드 화면 내부 키도 범위를 표시해 보여 주며 일반 게임플레이 충돌에서 제외합니다. 읽기 전용 Console 열기 키를 표시합니다.
- 윈도우 기본 UI 글꼴을 사용하고, 한글은 윈도우에 설치된 맑은 고딕, 중문은 Microsoft YaHei/JhengHei로 표시합니다. 별도 글꼴 파일을 배포하지 않습니다. 따뜻한 고대비 글자 옵션을 추가하며 기존 색상이 기본값입니다.
- SKSE/MCM, controlmap, ReShade tuple, Community Shaders의 확인된 저장 형식에 단축키 해제를 추가했습니다. 단일 숫자만 저장하는 설정의 조합키 및 불명확한 해제 값은 저장을 거부합니다.
- 최초 `.uhi.bak`에서 단축키 하나를 복원하고, 백업 탭에서 최근 256건의 UHM 변경 내역을 복원할 수 있습니다. 해제로 목록에서 사라진 기능도 내역에서 복원하며, 현재 값이 바뀌었으면 덮어쓰지 않습니다. 세이브에 저장되는 MCM 변경은 게임 저장이 필요합니다.
- 전용 단축키 문서, 중첩 JSON/YAML 키맵, 정수 값으로 내보낸 소수 형식 및 모드 이름 접두사가 붙은 실시간 MCM 속성 탐지를 보강했습니다. 네이티브 키맵은 대응 DLL의 PE 입력 API 근거도 확인합니다.
- Community Shaders의 실제 DLL 로드 상태를 스캔·캐시 복원 후 확인합니다. 이전 캐시를 무효화하고, 미검증 게임 버전의 런타임 배열 접근을 차단하며, 렌더러 실패 시 다른 모드의 창 프로시저 연결을 보존합니다.
- SE/AE 전용 빌드입니다. OAR·Heart of Magic 제보는 작성자가 UHM 비활성 상태에서도 재현됐다고 정정했으므로 원인 해결로 단정하지 않습니다. 실제 게임 호환성 확인은 별도로 필요합니다.

## 1.0.6 - 2026-08-28

- 네이티브 폰트 아틀라스를 지원 언어인 영문·한글·간체 중문 범위로 제한하고 사용하지 않는 일본어 글리프 범위를 제거했습니다. D3D11 폰트 텍스처 사전 검증을 추가해 지원하지 않는 아틀라스는 첫 메뉴 프레임에서 CTD를 일으키지 않고 안전하게 비활성화됩니다.
- Dear ImGui Win32/DX11 초기화를 창 열기 단축키 경로에서 제거하고 검증된 Skyrim D3D 초기화 훅으로 옮겼습니다. 렌더러가 완전히 준비된 뒤에만 네이티브 메뉴를 등록하므로, 너무 이른 시점이나 지원하지 않는 런타임에서 `Del`을 눌러도 보이지 않는 메뉴가 게임만 멈추거나 다음 입력에서 CTD가 발생하지 않고 안전하게 열기 요청을 거부합니다.
- SKSE Menu Framework 런타임 의존성을 제거하고 UHM 자체 Skyrim `IMenu`와 정적 Dear ImGui Win32/DX11 호스트로 전환했습니다. 기존 레이아웃·글자 크기·언어·팝업·캐시·스캐너·편집 기능은 그대로 유지합니다.
- 직접 단축키로 열 때 마우스·키보드·게임패드 입력이 Skyrim 메뉴 컨트롤까지 전달되도록 소유권을 수정했습니다. Escape는 활성 팝업·입력만 먼저 취소하거나 UHM만 닫으며 Skyrim 시스템 메뉴로 새지 않습니다.
- 리소스·기본값 제외와 파일별 캐시는 유지하면서 활성 VFS의 `Data` 아래에 있는 지원 설정 파일 전체로 탐색 범위를 확장했습니다.
- `_Key`, `_Mod*`, `_GPKey`, `_GPMod*` 같은 키보드·게임패드 복합 설정 묶음과 전용 입력 섹션 안의 일반 기능명을 모드 이름과 무관하게 탐지합니다.
- 사용자 정의 getter를 통과하는 PEX 데이터 흐름을 보강해 MCM·런타임 속성이 키맵 등록 API에 도달할 때 실제 기능 의미를 유지합니다.
- 활성 런타임 근거가 확인되지 않은 정적 PEX·DLL 입력 후보는 숨김·읽기 전용으로 유지해 분석기 내부 값이 단축키로 노출되지 않도록 했습니다.
- 탐색·장면 시작·가구·위젯·단계 흐름·네이티브 MCM 계열의 대표 구조를 익명 회귀 샘플로 검증했으며, 모드명 전용 탐지 규칙은 추가하지 않았습니다.
- UHM 열기·닫기, 메뉴 표시 상태 조회, UHM 자체 열기 단축키만 비활성화할 수 있는 외부 C API를 추가하고 소비자 헤더와 `GetProcAddress` 예제를 소스에 포함했습니다.
- 일반·팝업 버튼을 중성 회색으로 통일하고 입력칸·슬라이더·진행 막대의 은은한 외곽선과 흰색 탭 구분선을 적용하면서 탭과 장치 보기의 카테고리 색상은 유지했습니다.
- 네이티브 목록의 마우스 휠 스크롤을 수정했습니다. 게임 입력 싱크·메뉴·윈도우 프로시저로 들어오는 휠 이벤트를 프레임당 하나의 ImGui 휠 입력으로 모아 전달합니다.
- 옵션의 글자 크기·창 투명도 레이블에서 숫자 범위 표기를 제거했습니다. 실제 허용 범위인 80~135%와 35~100%는 그대로입니다.
- 이전 스캔 복원 안내 팝업을 크게 키우고 무채색 테두리를 적용했으며, 자동 닫힘 시간은 팝업이 실제로 표시된 뒤부터 셉니다.
- 카테고리 탭 아래에 중복으로 그려지던 얇은 선을 제거하고 흰색 구분선 하나만 남겼습니다.

## 1.0.5 - 2026-08-26

- 게임 버전별 ControlMap 입력 컨텍스트 경계를 수정했습니다. Skyrim 1.5.97은 17개 컨텍스트를, Marketplace가 추가된 Skyrim 1.6.1130 이상은 18개 컨텍스트를 읽습니다.
- SE/AE 런타임 인덱스 변환과 경계 회귀 테스트를 추가해 단축키 스캔 범위는 유지하면서 잘못된 컨텍스트 접근과 CTD를 방지했습니다.
- 이전 스캔 복원 안내를 오버뷰 본문에서 제거하고, 흰색 딤 처리 없이 한 번만 표시되는 중앙 팝업으로 옮겼습니다.

## 1.0.4 - 2026-08-26

- CommonLibSSE-NG v6.7.0 고정 릴리스로 빌드를 갱신하고 SE/AE에서 안전한 입력 sink 경로를 점검했습니다.
- vcpkg baseline, CommonLib 아카이브 체크섬, 재현 가능한 vcpkg 부트스트랩 리비전을 고정했습니다. 업스트림 전이 의존성 묶음은 개별 교체 없이 유지합니다.
- 공개 GitHub 소스 배포에 맞춰 라이선스·서드파티·배포 문서를 정리하고, 최신 CommonLibSSE-NG 링크와 Ko-fi 후원 링크를 반영했습니다.

## 1.0.3 - 2026-08-23

- 직접 창 열기 단축키의 입력 소유권을 수정했습니다. UHM 창 열기·닫기를 Skyrim UI 작업 큐에서 처리하고 해당 입력을 소비하며 첫 ImGui 프레임에서 포커스를 요청합니다. Menu Framework 패널 밖에서 열었을 때 창만 보이고 커서·입력이 넘어가지 않던 문제를 해결합니다.
- Menu Framework의 복구 진입점은 유지하고 버튼 명칭을 **Universal Hotkey Manager**로 정리했습니다.

## 1.0.2 - 2026-08-23

- ESC 입력 소유권, 팝업 연결과 입력 차단을 수정했습니다. 편집기는 저장 없이 닫히고, 팝업이 없을 때는 UHM만 닫히며 스카이림 시스템 메뉴가 함께 열리지 않습니다.
- 첫 화면 전에 검증된 캐시를 복원하고 완료된 세이브 로드 뒤 바뀐 근거만 갱신합니다. 큰 DLL에서도 3단계가 멈추지 않도록 제한하되 다른 단축키 근거 분석은 유지했습니다.
- 런타임 ControlMap 사용자 변경값을 loose·내장 기본값보다 먼저 적용하고 콘솔·디버그·Creation/마켓 관련 조작은 제외했습니다.
- 옵션과 키보드·마우스·게임패드 화면을 정리하고 투명도 저장, 카테고리 표현, 간결한 명칭, 툴팁, 명칭·단축키 편집 동작을 보강했습니다.
- Windows UTF-8 베타 로케일 없이 한글·중문 글리프를 준비하며 Menu Framework의 선택 폰트와 다른 설정은 보존하도록 수정했습니다.
- DirectInput·SKSE 통합 코드·Windows VK·XInput/controlmap·외부 도구·문서·MCM 저장 및 롤백 경로를 점검하고 GPLv3 배포 정보와 GitHub 소스를 정리했습니다.

## 1.0.1 - 2026-08-23

- 장치 보기와 모든 단축키 관리 목록에서 명칭 변경·단축키 변경 팝업 요청이 유실되지 않도록 수정했습니다.
- 편집·알림 팝업을 한 번만 표시하고 중앙 정렬·입력 차단·ESC 저장 없는 취소를 적용했습니다.
- 다중 바인딩 툴팁, 팝업 진단 로그, 출처별 저장 점검, 실제 화면 이미지와 영문·한글 문서를 추가했습니다.

## 1.0.0 - 2026-08-23

- 공개 명칭을 **Universal Hotkey Manager for Skyrim SE-AE**로 확정하고, ESP 없는 Skyrim SE/AE용 SKSE·Menu Framework 배포 구성을 완성했습니다.
- 오버뷰, 게임 기본, 게임 환경, 캐릭터, 전투, SexLab, 인터페이스, 외부 도구, 옵션 화면을 완성했습니다. 카테고리 색상, 장치 보기, 정렬 가능한 목록, 검색·장치 필터, 충돌·중복 필터, UI 배율, 자동·수동 한글/영문/중문 선택을 제공합니다.
- 키보드·마우스·Xbox형 게임패드 장치도를 구현했습니다. 키 조합, 카테고리별 색상, 여러 바인딩 툴팁, 명칭 변경과 단축키 변경 인터페이스를 포함합니다.
- 설정·아카이브, Papyrus PEX/MCM, 네이티브 DLL·활성 입력 핸들러, 저장 등록·외부 도구를 순서대로 검사하는 취소 가능한 4단계 스캐너를 추가했습니다. Bethesda 기본 게임·DLC·CC 아카이브는 열지 않고, 유효한 loose `controlmap.txt`가 없으면 내장 기본 조작표를 사용합니다.
- DirectInput 스캔코드, SKSE 통합 입력 코드, Windows VK, XInput/controlmap, ReShade, Community Shaders, ENB, 기호형 조합과 INI·JSON·JSONC·TOML·YAML·XML·CFG·CONF·SETTINGS·PROPERTIES 형식을 해석합니다.
- SkyUI MCM 데이터 흐름, MCM Helper·사용자 정의 getter, PEX 속성·배열·상태·계산값, 실시간 Papyrus 값과 co-save 등록 분석을 확장했습니다. Defeat F2, Typing Mode F4, Conditional Expressions Extended F8, SexLab 계열의 복구 경로를 포함합니다.
- 설정 항목, 설명, 섹션, 함수, 속성, 번역, 런타임 등록과 소유자 근거를 조합해 기능명을 복원하고, 내부 접두사·번역 토큰·URL·숫자 메타데이터·불명확한 이름을 정리합니다. 사용자는 표시 명칭을 직접 바꿀 수 있습니다.
- 플러그인 마스터 관계, 모드 식별자, 파일명, 스크립트·DLL·설정 경로와 기능 근거를 이용한 카테고리 분류기를 추가했습니다. SexLab 장면 전용 및 특정 UI 내부 전용 입력은 일반 충돌과 분리합니다.
- 물리 장치·키와 활성 컨텍스트 교집합을 기준으로 충돌 판정을 다시 구성했습니다. 양쪽 컨텍스트가 확정되어 겹칠 때만 빨간 충돌, 불명·추정이 포함되면 주황 중복으로 표시하고, 확실히 분리된 컨텍스트와 아날로그 축·스틱 클릭은 충돌로 처리하지 않습니다.
- 출처 형식에 맞춘 단축키 저장, 값 변경 확인, 원자적 파일 교체와 최초 1회 `.uhi.bak` 백업을 추가했습니다. 손실 없는 변환이 불가능하면 저장을 거부합니다.
- MCM 동기화를 Skyrim 메인 작업에서 수행합니다. 문서 기반 값과 MCM 동기화가 실패하면 원래 키로 되돌리고 알리며, 세이브 기반 Papyrus/MCM 값은 게임 저장이 필요하다는 알림을 표시합니다.
- 스캔 결과를 Skyrim 세이브와 별개로 즉시 저장하고, 파일별 캐시와 화면 복원 스냅샷을 추가했습니다. 완료된 세이브 로드 뒤에는 바뀐 파일만 증분 검사하고, 변경된 활성 단축키가 있을 때만 알립니다.
- 로드·리버트·세이브 삭제·스캔 취소·팝업 입력·VM 근거·활성 CommonLib sink·메인 스레드 저장의 안전 가드를 추가했습니다. 런타임 함수 디투어나 주소 패치는 사용하지 않습니다.
- `UniversalHotkeyManager.ini`를 통한 창 열기 키·UI 배율·언어 복구, 캐시 삭제 안내, 간략 사용법, 영문 Nexus BBCode, 한글 HTML 소개글, 설치 문서와 릴리즈·소스 패키징을 정리했습니다.
- 스캐너, 캐시 무효화, 입력 코드 해석·저장, controlmap 장치, MCM 동기화, 컨텍스트·충돌 판정, 분류, UI 설정, 취소와 안전 한계를 다루는 회귀 테스트를 확장했습니다.

## 0.0.1 - 2026-08-14 (프리빌드)

- ESP 없는 SKSE 플러그인, Menu Framework 창, 변경 가능한 창 열기 단축키와 최초 키보드·마우스·게임패드 오버뷰를 구성했습니다.
- 문서, PEX, DLL, 저장 데이터, 외부 오버레이와 loose `controlmap.txt`를 검사하는 초기 4단계 스캐너 및 캐시·취소 구조를 만들었습니다.
- 초기 입력 코드 해석, 카테고리, 검색, 장치 필터, 충돌 표시, 출처 근거, Bethesda 기본 아카이브 제외, 패키징과 자동 테스트를 추가했습니다.
